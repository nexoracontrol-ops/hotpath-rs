# Used by: open-source Rust projects profiling with hotpath

These public GitHub repositories declare `hotpath` as a dependency in their `Cargo.toml`. The list is refreshed daily from a GitHub code search and sorted by stars.

{{#used_by}}

Using hotpath in your project? It will show up here automatically once the dependency is public on GitHub.
